[pytest]
markers =
    regression: regression tests with determinism and baseline checks
    smoke: smoke tests for basic route functionality
    security: security-related tests (PII, XSS, credentials)
    integration: integration tests across multiple components
    unit: unit tests for isolated functionality
testpaths = tests .
addopts = -ra
